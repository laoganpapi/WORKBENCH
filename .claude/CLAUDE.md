# Workbench facts

Purpose: default home for research, analysis, writing, and repo-less work.
Stack: markdown + ad-hoc python3; no build system.
Test command: none — no code project; fabled-code arms ad-hoc checks only.
Lint command: none.
Stakes threshold: default (irreversible, external-facing, or >5 files).
Storage ladder lives per domain directory (research/ analysis/ writing/):
timestamped structured files → SQL → graph; escalate only via /retro flags.
